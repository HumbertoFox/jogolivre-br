'use client';

export default function ObjectiveClient() {
    return (
        <section id="objective" className="min-w-full min-h-[70svh] flex flex-col items-center pt-24 px-8">
            <h1>Nosso Objetivo!</h1>
        </section>
    );
}